import Link from 'next/link'
import { notFound } from 'next/navigation'
import { ArrowLeft, Bookmark, BriefcaseBusiness, CalendarClock, CheckCircle2, MapPin, Send, Share2, Sparkles } from 'lucide-react'
import { AppShell } from '@/components/app-shell'
import { jobs } from '@/lib/mock-data'

export default function JobDetailPage({ params }: { params: { slug: string } }) {
  const job = jobs.find(item=>item.slug===params.slug)
  if (!job) notFound()
  return <AppShell><main className="mx-auto max-w-6xl px-4 py-10 lg:px-8"><Link href="/" className="inline-flex items-center gap-2 text-sm font-semibold text-muted-foreground hover:text-primary"><ArrowLeft className="size-4"/>返回职位列表</Link><div className="mt-6 grid gap-6 lg:grid-cols-[1fr_330px]"><article className="space-y-6"><section className="paper-card p-7"><div className="flex flex-col gap-5 sm:flex-row sm:items-start"><div className="grid size-16 shrink-0 place-items-center rounded-2xl bg-gradient-to-br from-amber-100 to-lime-100 text-2xl font-black text-amber-800">{job.companyLogo}</div><div className="flex-1"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="eyebrow">{job.isNew?'今日新增':'校招岗位'}</div><h1 className="mt-2 text-3xl font-black tracking-tight">{job.title}</h1></div><div className="flex gap-2"><button className="grid size-10 place-items-center rounded-xl border bg-white"><Share2 className="size-4"/></button><button className="grid size-10 place-items-center rounded-xl border bg-white"><Bookmark className="size-4"/></button></div></div><div className="mt-4 flex flex-wrap gap-4 text-sm text-muted-foreground"><span className="flex items-center gap-1.5"><BriefcaseBusiness className="size-4"/>{job.company}</span><span className="flex items-center gap-1.5"><MapPin className="size-4"/>{job.cities.join(' / ')}</span><span className="flex items-center gap-1.5"><CalendarClock className="size-4"/>截止 {job.deadline}</span></div><div className="mt-5 text-xl font-black text-primary">{job.salary}</div></div></div></section><Section title="职位介绍"><p className="leading-8 text-muted-foreground">{job.description}</p></Section><Section title="岗位职责"><List items={job.responsibilities}/></Section><Section title="任职要求"><List items={job.requirements}/></Section><Section title="岗位亮点"><div className="grid gap-3 sm:grid-cols-3">{job.highlights.map(x=><div key={x} className="rounded-xl bg-lime-50 p-4 text-sm font-semibold text-lime-900"><CheckCircle2 className="mb-3 size-5 text-lime-600"/>{x}</div>)}</div></Section></article><aside className="space-y-5"><section className="paper-card sticky top-24 p-6"><div className="flex items-center gap-2 text-sm font-bold text-amber-700"><Sparkles className="size-5"/>AI 岗位匹配</div><div className="mt-5 flex items-end gap-2"><span className="text-5xl font-black">{job.matchScore}</span><span className="pb-1 text-lg font-bold text-muted-foreground">/ 100</span></div><div className="mt-4 h-2.5 overflow-hidden rounded-full bg-secondary"><div className="h-full rounded-full bg-gradient-to-r from-amber-400 to-lime-400" style={{width:`${job.matchScore}%`}}/></div><p className="mt-5 text-sm leading-6 text-muted-foreground">你的目标方向、城市偏好与岗位高度一致。建议突出 AI 产品实践和数据分析项目。</p><div className="mt-5 space-y-2">{job.tags.map(x=><div key={x} className="flex items-center gap-2 text-sm"><CheckCircle2 className="size-4 text-lime-600"/>{x} 匹配</div>)}</div><button className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl bg-primary py-3 text-sm font-bold text-primary-foreground shadow-lg"><Send className="size-4"/>标记为已投递</button><button className="mt-3 w-full rounded-xl border bg-white py-3 text-sm font-bold">查看招聘官网</button></section></aside></div></main></AppShell>
}

function Section({title,children}:{title:string,children:React.ReactNode}) { return <section className="paper-card p-7"><h2 className="text-xl font-black">{title}</h2><div className="mt-5">{children}</div></section> }
function List({items}:{items:string[]}) { return <div className="space-y-3">{items.map((x,i)=><div key={x} className="flex gap-3 leading-7 text-muted-foreground"><span className="grid size-6 shrink-0 place-items-center rounded-full bg-amber-100 text-xs font-bold text-amber-800">{i+1}</span><span>{x}</span></div>)}</div> }
